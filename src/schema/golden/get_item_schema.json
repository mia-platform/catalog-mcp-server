{
  "additionalProperties": false,
  "properties": {
    "fields": {
      "description": "Only these fields' schema, e.g. [\"spec.lifecycle\"]. Absent returns the whole definition.",
      "items": {
        "type": "string"
      },
      "type": [
        "array",
        "null"
      ]
    },
    "group": {
      "description": "The kind's API group. Only needed when several types share the kind.",
      "type": [
        "string",
        "null"
      ]
    },
    "kind": {
      "description": "The type's kind, e.g. \"Service\".",
      "type": "string"
    },
    "version": {
      "description": "A specific version. Absent means the served one.",
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "kind"
  ],
  "type": "object"
}
